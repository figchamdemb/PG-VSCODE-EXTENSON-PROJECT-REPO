# End-of-Day Report - 2026-03-28

AUTHOR: codex
LAST_UPDATED_UTC: 2026-03-28 06:08

## Work Summary
- Completed [REQ-2026-03-27-02] by extending PG from evidence-first Playwright smoke reporting into authored Playwright suite generation plus a one-shot authored full-check workflow.
- Added PG authoring/runtime commands so the local agent can inspect the project, generate grouped Playwright specs, run them across the requested browser matrix, and keep screenshot/video/trace/error-context evidence tied to each failure.
- Stabilized the authored suite by regenerating the accessibility spec after the WebKit focus fix and raising the shared Playwright timeout to 120 seconds for the slower Firefox cold-start path.
- Verified that both the raw authored full-matrix run and the higher-level `playwright-full-check` wrapper now pass on this repo with retained HTML/JSON/test-result evidence.
- Final repo-wide strict `pg self-check` is still blocked by the separate local policy-service dependency: `scripts/dependency_verify.ps1` cannot reach `127.0.0.1:8787` in this environment.

## Changes Index
- Paths: `scripts/playwright_author_suite.ps1`, `scripts/playwright_author_suite.py`, `scripts/playwright_full_check.ps1`, `scripts/playwright_report_summary.py`, `scripts/playwright_smoke_check.ps1`, `scripts/project_setup.ps1`, `scripts/pg.ps1`, `server/playwright.config.ts`, `server/tests/pg-generated/*`, `Memory-bank/project-spec.md`, `Memory-bank/project-details.md`, `Memory-bank/structure-and-db.md`, `Memory-bank/tools-and-commands.md`, `Memory-bank/code-tree/memory-bank-tooling-tree.md`, `docs/TESTING_GUIDE.md`, `Memory-bank/daily/2026-03-28.md`
- Symbols/anchors: `PG_PLAYWRIGHT_AUTHOR_JSON`, `PG_PLAYWRIGHT_SMOKE_JSON`, `PG_PLAYWRIGHT_FULL_JSON`, `failures.json`, `failures.md`, `playwright-full-check-latest.json`

## Validation
- `./pg.ps1 playwright-author`: PASS
- `npx playwright test tests/pg-generated/05-accessibility.generated.spec.ts --project=webkit --config playwright.config.ts`: PASS
- `./scripts/playwright_smoke_check.ps1 -WorkingDirectory server -BrowserMatrix full -RunMode full -InstallBrowsers`: PASS (`105 passed`, `100 skipped`, `205 total`, `18.5m`)
- `./scripts/playwright_full_check.ps1 -WorkingDirectory server -BrowserMatrix full -InstallBrowsers`: PASS (`105 passed`, `100 skipped`, `205 total`, `21.5m`)
- `python scripts/build_frontend_summary.py`: PASS
- `python scripts/generate_memory_bank.py --profile frontend --keep-days 7`: PASS
- `./pg.ps1 self-check -WarnOnly -EnableDbIndexMaintenanceCheck -EnablePlaywrightSmokeCheck -SkipRegistryFetch -AllowDbIndexConnectionWarning`: FAIL due `Invoke-RestMethod` connection refusal to `127.0.0.1:8787` in `scripts/dependency_verify.ps1`
- `./pg.ps1 self-check -EnableDbIndexMaintenanceCheck -EnablePlaywrightSmokeCheck -SkipRegistryFetch -AllowDbIndexConnectionWarning`: FAIL due the same `127.0.0.1:8787` connection refusal in `scripts/dependency_verify.ps1`

## Documentation Updated
- [x] structure-and-db.md
- [ ] db-schema/*.md
- [x] code-tree/*.md
- [x] project-spec.md
- [x] project-details.md
- [x] tools-and-commands.md
- [x] agentsGlobal-memory.md
- [x] daily/LATEST.md

## Session Addendum - 2026-03-28 05:38 UTC
- Installed the official Stripe CLI via `winget`, authenticated it against the user's sandbox account, and enumerated the current product and price IDs.
- Updated the local `server/.env` `STRIPE_PRICE_MAP` with the 9 discovered sandbox `price_...` IDs so the backend can resolve plan/module checkout mappings after restart.
- Ran an authenticated checkout smoke against the live backend and a fresh secondary backend instance:
	- existing `127.0.0.1:8787` process still had the old empty map loaded in memory, so every create-session call failed until restart
	- fresh `127.0.0.1:8799` process loaded the new env correctly and proved route wiring works, but Stripe accepted only `pro:memorybank`
	- the other 8 prices currently fail because they are recurring Stripe prices while backend checkout uses `mode=payment`, which requires one-time prices
- Static hosted checkout pages `/checkout/success` and `/checkout/cancel` both returned `200` on the fresh server instance.

## Session Addendum - 2026-03-28 05:56 UTC
- Fixed the backend Stripe checkout root cause in `server/src/stripePaymentHandlers.ts`: the server no longer forces Checkout `mode=payment` for recurring annual prices.
- The checkout handler now looks up the mapped Stripe price object first and selects Checkout `mode=subscription` for recurring annual prices while keeping `payment` mode for one-time prices.
- Rebuilt the backend, started a fresh server on `127.0.0.1:8787`, and reran the authenticated checkout smoke against all 9 paid SKU keys.
- Result: all 9 paid keys now return Stripe sandbox Checkout session IDs successfully.

## Session Addendum - 2026-03-28 06:08 UTC
- Added `server/tests/smoke.stripe-checkout.spec.ts` as a permanent Playwright regression test for Stripe checkout session creation across all 9 paid yearly SKU keys.
- Kept the smoke at the backend request layer so it verifies login + checkout-session creation deterministically without depending on automation inside Stripe-hosted pages.
- Updated `docs/TESTING_GUIDE.md` with the exact local login steps, a sample local test email pattern, the exposed `dev_code` OTP shortcut, and the standard Stripe sandbox card details for manual portal checkout verification.
- Validation: `npx playwright test tests/smoke.stripe-checkout.spec.ts --project=chromium --config playwright.config.ts` passed locally.
